let todoList = [{ 'task' : "wash car", 'prio' : "low", 'id': 1},{ "task" : "do dishes", "prio" : "medium", 'id': 2}, { 'task' : "doctor's appointment", "prio": "high", 'id': 3} ]

module.exports = todoList;

// import postgres from 'postgres'

// const sql = postgres({ /* options */ }) // will use psql environment variables

// export default sql